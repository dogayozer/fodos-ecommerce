import type { NextConfig } from 'next'; const nextConfig: NextConfig = { images: { remotePatterns: [ { protocol: 'https', hostname: '**' } ], unoptimized: true /* Vercel Görsel Optimizasyonu kotası (OPTIMIZED_IMAGE_REQUEST_PAYMENT_REQUIRED / 402) 14bin+ üründeki binlerce farklı harici görsel yüzünden tükendi; kota dolunca sitedeki tüm görseller yüklenemez hale geliyordu. Optimizasyon kapatılınca görseller doğrudan kaynak URL'den servis edilir, kota devre dışı kalır. */ }, skipTrailingSlashRedirect: true, rewrites: async () => { return { beforeFiles: [ { source: '/api/orderStatus/', destination: '/api/orderStatus' }, { source: '/api/paymentMethods/', destination: '/api/paymentMethods' }, { source: '/api/orders', destination: '/api/birfatura/orders' }, { source: '/api/orders/', destination: '/api/birfatura/orders' } ] }; } }; export default nextConfig;
