version: '3.8'

services:
  meilisearch:
    image: getmeili/meilisearch:v1.6
    ports:
      - "7700:7700"
    environment:
      - MEILI_MASTER_KEY=FodosMasterKey123!
    volumes:
      - ./meili_data:/meili_data
    restart: unless-stopped
